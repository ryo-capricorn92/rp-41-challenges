var subtotal = 'n'

function billTotal(subtotal, tax, tip){
	return subTotal(.095) * .015;
}

tax(.095);
tip(.015);

var animal ='cat'
var emotion = 'hungry' || ''

function animalNoise(animal, emotion){
	if(cat, hungry){
		console.log('meow');
	}
}
